apiVersion: v1
kind: Service
metadata:
  name: tomato-health-service
  labels:
    app: tomato-health-api
spec:
  type: NodePort
  selector:
    app: tomato-health-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
